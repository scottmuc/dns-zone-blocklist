{
  "name": "dns-zone-blocklist",
  "version": "3.0.0",
  "description": "A tool to convert host file data to bind, dnsmasq, and unbound configuration.",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Scott Muc <scott@scottmuc.com>",
  "license": "MIT",
  "dependencies": {
    "ejs": "^2.6.1",
    "request": "^2.88.0",
    "request-promise": "^4.2.2",
    "tlds": "^1.221.0"
  }
}
